import { BedMarkedDirective } from './bed-marked.directive';

describe('BedMarkedDirective', () => {
  it('should create an instance', () => {
    const directive = new BedMarkedDirective();
    expect(directive).toBeTruthy();
  });
});
