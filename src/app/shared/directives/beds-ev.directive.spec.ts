import { BedsEvDirective } from './beds-ev.directive';

describe('BedsEvDirective', () => {
  it('should create an instance', () => {
    const directive = new BedsEvDirective();
    expect(directive).toBeTruthy();
  });
});
