import { RoomsEvDirective } from './rooms-ev.directive';

describe('RoomsEvDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomsEvDirective();
    expect(directive).toBeTruthy();
  });
});
