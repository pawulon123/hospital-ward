import { MoveBedDirective } from './move-bed.directive';

describe('MoveBedDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveBedDirective();
    expect(directive).toBeTruthy();
  });
});
