import { InnerBedDirective } from './inner-bed.directive';

describe('InnerBedDirective', () => {
  it('should create an instance', () => {
    const directive = new InnerBedDirective();
    expect(directive).toBeTruthy();
  });
});
